# Experiment Runners

Run these scripts from the repository root, for example:

```bash
bash runners/run_uncertainty_guided_experiments.sh
```

The main experiment runner remains at the repository root:

```bash
bash run_main_experiments.sh
```
