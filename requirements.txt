# Tested with Python 3.11 in the thesis GPU environment.
# Install the PyTorch build matching your CUDA/CPU platform first if needed:
# https://pytorch.org/get-started/locally/
torch>=2.0
torchvision>=0.15

numpy>=1.23,<2.0
tqdm>=4.64
Pillow>=9.3
h5py>=3.8
timm==0.9.12
ivon-opt==0.1.3
matplotlib>=3.7
scikit-learn>=1.2
pytest>=8.0
